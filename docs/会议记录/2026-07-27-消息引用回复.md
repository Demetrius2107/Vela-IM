# Vela IM — 会话工作记录

> 日期：2026-07-27
> 分支：feat/message-reply
> 状态：开发进行中

---

## 本次完成工作

### 消息引用回复 — 数据模型

| 文件/模块 | 改动说明 |
|-----------|----------|
| `.../types/message/MessageContent.java` | 新增 `replyToMsgKey` / `replyToFromId` / `replyToMsgBody` 三个字段 |
| `.../types/message/OfflineMessageContent.java` | 同步新增同名字段，确保离线消息也携带回复上下文 |

### 设计说明

引用回复的实现策略：**透传**。

- 客户端发送消息时填入 `replyToMsgKey`（被回复消息ID）+ `replyToFromId`（发送方）+ `replyToMsgBody`（正文预览）
- 管道节点（Validate→RateLimit→Dedup→PersistAndPush）对 `MessageContent` 泛型操作，新字段自动透传
- `BeanUtils.copyProperties` 在离线消息存储时自动复制回复字段到 `OfflineMessageContent`
- 接收端拿到消息后根据 `replyToMsgKey` 展示引用关系

### Git 提交记录

```
e7d0661 feat(im-service-group): 群公告功能——实体/CRUD/已读统计
bc1cf0d feat(im-common): @提及——MessageContent增加mention字段
1b3ae6f feat(im-common): 消息转发——MessageContent增加forwardFrom字段
a7526f3 feat(im-common): 消息引用回复——MessageContent增加replyTo字段
```

### 当前阶段进展

| 功能 | 状态 |
|----|----|
| 消息引用回复 | ✅ 数据模型完成 |
| 消息转发 | ✅ 数据模型完成 |
| @提及 | ✅ 数据模型完成 |
| 群公告 | ✅ 后端完整实现（实体+CRUD+已读统计） |
| 表情/贴纸 | ⏳ 待启动 |

### 群公告接口

| 端点 | 方法 | 说明 |
|------|------|------|
| `/v1/group/announcement/publish` | POST | 发布公告（自动更新群表 notification） |
| `/v1/group/announcement/list` | GET | 公告列表（按时间降序） |
| `/v1/group/announcement/markRead` | POST | 标记已读（幂等） |
| `/v1/group/announcement/readStats` | GET | 已读统计（已读/未读/总数） |
| `/v1/group/announcement/delete` | POST | 逻辑删除公告 |

### Git 提交记录（Phase 1 汇总）

```
cdf586e feat(im-common,im-service): 贴纸消息——实体/CRUD/消息透传
e7d0661 feat(im-service-group): 群公告功能——实体/CRUD/已读统计
bc1cf0d feat(im-common): @提及——MessageContent增加mention字段
1b3ae6f feat(im-common): 消息转发——MessageContent增加forwardFrom字段
a7526f3 feat(im-common): 消息引用回复——MessageContent增加replyTo字段
```

### Phase 1 完成情况

| 功能 | 状态 |
|----|----|
| 消息引用回复 | ✅ 数据模型完成 |
| 消息转发 | ✅ 数据模型完成 |
| @提及 | ✅ 数据模型完成 |
| 群公告 | ✅ 后端完整实现 |
| 表情/贴纸 | ✅ 后端完整实现 |
| **Phase 1 合计** | **5/5 ✅ 全部完成** |

### Phase 2 进展

| 功能 | 状态 |
|----|----|
| 图片消息 | ✅ 文件存储 + 消息字段 + 上传接口 |
| 文件消息 | ✅ 与图片共享基础设施 |
| 视频消息 | ✅ 上传端点 + fileType 透传 |
| 语音消息 | ✅ 上传端点 + fileType 透传 |
| 位置消息 | ✅ 坐标字段 + 消息透传 |
| 名片消息 | ✅ 名片字段 + 消息透传 |
| **Phase 2 合计** | **6/6 ✅ 全部完成** |

### 新增接口

| 端点 | 方法 | 说明 |
|------|------|------|
| `/v1/file/upload/image` | POST | 上传图片（校验格式/大小） |
| `/v1/file/upload/video` | POST | 上传视频 |
| `/v1/file/upload/audio` | POST | 上传语音 |

### Phase 3 进展

| 功能 | 状态 |
|----|----|
| @所有人 | ✅ 权限校验（仅管理员/群主触发） |
| 群二维码/邀请链接 | ✅ 链接生成 + Redis 过期令牌 + 扫码加群 |
| 群投票 | ✅ 实体/CRUD/投票/结果统计 |
| 已读成员列表 | ✅ 消息级已读跟踪 |
| 群标签/分类 | ✅ 实体/CRUD |

### Phase 4 — 音视频通话

| 功能 | 状态 |
|----|----|
| WebRTC 信令指令 | ✅ MessageCommand 新增 6 条指令 |
| 通话状态管理 | ✅ CallSessionManager（响铃/接通/结束） |
| 信令转发服务 | ✅ CallSignalingService（Offer/Answer/ICE 转发） |
| 前端 WebRTC | ✅ PeerConnection + 媒体流 + 信令交互 |
| 通话 UI | ✅ CallPanel（响铃/呼叫中/视频/语音/挂断） |

### Git 提交记录（Phase 4）

```
618167c feat(im-service,web): 管理后台+Bot机器人系统
43f9b73 feat(im-common,im-service,web): WebRTC音视频通话——信令+通话管理+前端UI
```

### Phase 5 进展

| 功能 | 状态 |
|----|----|
| 管理后台 | ✅ 后端API + 前端页面（看板/用户/群组） |
| Bot 机器人 | ✅ 完整模块（回复/指令/管理/Webhook） |
| 端到端加密 | 📋 |
| 开放 API/SDK | 📋 |

---

> 文档版本: v1.8 | 创建时间: 2026-07-27 | 更新: Phase 5 管理后台+Bot

### 新增接口

| 端点 | 方法 | 说明 |
|------|------|------|
| `/v1/group/join/invite` | POST | 生成带过期时间的邀请令牌 |
| `/v1/group/join/invite/accept` | POST | 通过令牌加群 |
| `/v1/group/join/link` | GET | 生成群邀请链接 |
| `/v1/group/join` | POST | 通过链接加群 |

---

> 文档版本: v1.6 | 创建时间: 2026-07-27 | 更新: Phase 3 @所有人+邀请链接
