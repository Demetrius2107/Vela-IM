# Vela IM — 会话工作记录

> 日期：2026-07-25
> 分支：feat/next-phase
> 状态：开发进行中

---

## 今日完成工作

### 1. 前端目录重构（`web/`）

| 模块 | 路径 | 说明 |
|------|------|------|
| API 层 | `web/src/api/` | Axios 封装 + user/friend/group/message 接口 |
| 状态管理 | `web/src/stores/` | Pinia 三模块（user/conversation/message） |
| 工具层 | `web/src/utils/` | constants 常量 / socket WebSocket 客户端 / theme 主题 |
| 布局组件 | `web/src/components/layout/NavHeader.vue` | 统一导航栏 + 头像下拉菜单 |
| 路由 | `web/src/router/index.js` | 全部页面路由 + 登录守卫 |

### 2. 核心页面实现

| 页面 | 路径 | 功能 |
|------|------|------|
| 登录 | `views/login/Index.vue` | 渐变背景 + 表单校验 + 跳转注册 |
| 注册 | `views/register/Index.vue` | 四字段注册 + 两次密码一致性校验 |
| 聊天 | `views/chat/Index.vue` | 会话列表/消息气泡/输入框组件化 |
| 通讯录 | `views/contacts/Index.vue` | 好友/群组/请求三 Tab + 在线状态 + 签名 |
| 个人资料 | `views/profile/Index.vue` | 头像/昵称/签名/性别编辑 |
| 用户搜索 | `views/search/Index.vue` | 搜索用户 + 好友申请 |
| 消息搜索 | `views/search/Message.vue` | 按关键词搜索聊天记录 |
| 群组详情 | `views/group/Index.vue` | 群成员列表/角色标签/信息编辑/解散 |
| 创建群组 | `views/group/Create.vue` | 群名称/介绍/选择成员 |
| 设置 | `views/settings/Index.vue` | 通知/头像显示/在线状态/账号信息 |
| 过渡页 | `views/splash/Index.vue` | 登录后 Vela 品牌动画 |

### 3. 聊天页增强（多次迭代）

| 功能 | 状态 |
|------|----|
| 会话筛选（全部/未读/群聊/好友） | ✅ |
| 左右栏可拖拽调整宽度（200-500px） | ✅ |
| 输入框可上下拖拽调整高度（80-200px） | ✅ |
| 消息右键菜单（复制/撤回/删除） | ✅ |
| 右上角三点展开右侧信息面板 | ✅ |
| 消息已读/送达状态（✓/✓✓） | ✅ |
| 在线状态绿点 + 离线灰色 | ✅ |
| 聊天气泡圆角区分左右 | ✅ |
| 输入栏工具栏（表情/图片/附件按钮） | ✅ |
| 毛玻璃输入框（backdrop-filter: blur(12px)） | ✅ |
| 个性签名展示（会话列表+聊天头部+右侧面板） | ✅ |
| 右侧面板扩充（共同群组/近期图片/更多信息） | ✅ |
| 日期分隔线（"今天""昨天"） | ✅ |

### 4. 全局 UI 美化

| 改动 | 说明 |
|------|------|
| Naive UI 主题覆盖 | `utils/theme.js` — 品牌色系 |
| 毛玻璃卡片 | `.glass-card` — backdrop-filter: blur(20px) |
| 苹果风格滚动条 | 窄滚动条 + 圆角滑块 |
| 字体平滑 | antialiased + grayscale |
| 渐变背景 | `linear-gradient(135deg, #f5f7fa, #e4e9f2)` |
| 浏览器标签 | "Vela IM" 标题 + zh-CN 语言 |
| package.json | `@vela/web` v0.1.0 |

### 5. 新增模块

| 模块 | 说明 |
|------|------|
| `vela-gateway/` | Spring Cloud Gateway API 网关（端口 8888） |
| `deploy/prometheus.yml` | Prometheus 采集配置 |
| `docs/guide/docker-operations-guide.md` | Docker 操作指南文档 |
| `docs/guide/docker-complete-guide.md` | Docker 全面指南文档 |
| `docs/guide/docker-troubleshooting.md` | Docker 踩坑记录文档 |

---

## Git 提交记录

### 分支 `feat/next-phase`（当前）

```
bfbfc3a  UI美化——全局毛玻璃/苹果极简风格/品牌统一
50277ad  新增功能页面——注册/资料/搜索/群组/设置/过渡
7537b57  核心页面——登录/聊天/通讯录完整实现
1075466  前端基础设施——API层/状态管理/工具库/路由重构
01174fd  Vue 3 + Naive UI 前端客户端（首次创建）
```

### 最后提交（当前）

```
d41b380  fix(web): 聊天页UI优化——签名展示/右侧面板扩充/输入框毛玻璃
```

---

## 当前项目结构

```
Vela/
├── vela-gateway/        ← 新增：API 网关
├── vela-service/        ← 后端业务服务
├── vela-tcp/            ← TCP/WS 网关
├── vela-message-store/  ← 消息存储
├── vela-common/         ← 共享内核
├── vela-codec/          ← 编解码
├── web/                 ← 新增：Vue 3 前端
│   ├── src/
│   │   ├── api/         ← 请求层
│   │   ├── stores/      ← 状态管理层
│   │   ├── utils/       ← 工具层
│   │   ├── components/  ← 组件层
│   │   └── views/       ← 页面层（11个页面）
│   └── ...
├── deploy/              ← 新增：部署配置
├── docs/
│   ├── guide/           ← Docker 指南文档
│   └── 会议记录/         ← 新增：会话工作记录
└── docker-compose.yml   ← 含网关+监控+前端
```

## 待办事项

| 优先级 | 事项 | 预估 |
|------|------|----|
| 🟡 | React（Modern.js）版本初版原型 | 远期 |
| 🟡 | 前端与后端实际接口联调 | 持续 |
| 🟢 | Android/iOS 原生客户端 | 远期 |
| 🟢 | 消息搜索对接 Elasticsearch | 后续 |

---

> 文档版本: v1.1 | 创建时间: 2026-07-25 | 最后更新: 2026-07-27

---

## 2026-07-27 会话：后端可靠性增强（P0 阶段）

> 基于差距分析文档，实现 L2 异常边界核心任务
> 分支：feat/next-phase | 领先 master 12 个提交

### 本次完成工作

#### 1. 消息发送指数退避重试机制

| 文件/模块 | 改动说明 |
|-----------|----------|
| `.../utils/RetryUtil.java` | **新建** — 指数退避重试工具类（默认3次: 100ms→500ms→2000ms），支持 Supplier/Runnable 双接口 |
| `.../pipeline/node/PersistAndPushNode.java` | P2P分发送接收方改由 RetryUtil 指数退避重试（可配） |
| `.../pipeline/node/GroupBroadcastNode.java` | 群聊成员分发改由 RetryUtil 指数退避重试（2次，100ms→1000ms） |
| `.../config/ImServerProperties.java` | 新增 `RetryConfig` 内部类（maxRetries/baseDelayMs/maxDelayMs） |

#### 2. ACK 丢失重推机制

| 文件/模块 | 改动说明 |
|-----------|----------|
| `.../utils/PendingAckTracker.java` | **新建** — 内存 ACK 跟踪器，按 toId+messageKey 管理待确认消息 |
| `.../utils/AckRetryScheduler.java` | **新建** — 定时任务（@Scheduled 5s），超时未 ACK 指数退避重推，3次耗尽后发服务端确认 |
| `.../pipeline/node/PersistAndPushNode.java` | 接收方在线时调用 `pendingAckTracker.track()` 开启跟踪 |
| `.../message/.../MessageSyncService.java` | `receiveMark()` 中调用 `pendingAckTracker.acknowledge()` 清除跟踪 |
| `.../Application.java` | 添加 `@EnableScheduling` |

#### 3. 消息撤回时间边界容错

| 文件/模块 | 改动说明 |
|-----------|----------|
| `.../config/ImServerProperties.java` | 新增 `messageRecallClockSkewTolerance` 可配置字段（默认5000ms） |
| `.../message/.../MessageSyncService.java` | clockSkewTolerance 可配置化；**新增反向时钟偏差检查**（clientTime 过度落后也拒绝）；日志增强 |
| `.../test/.../MessageSyncServiceTest.java` | 新增 5 个测试用例 |

#### 4. 基础设施 & 测试

| 文件/模块 | 改动说明 |
|-----------|----------|
| `pom.xml` | Lombok 升级至 1.18.34 以兼容 JDK21 |
| `.mvn/jvm.config` | **新建** — JVM --add-opens 配置 |
| `.../test/.../P2PMessageServiceTest.java` | 适配 PersistAndPushNode 新构造参数 |
| `.../test/.../MessageSyncServiceTest.java` | 适配 MessageSyncService 新构造参数 |

### Git 提交记录（本次新增 4 个提交）

```
f905c4e chore: 基础设施适配——Lombok升级+测试构造修复
84b8569 fix(im-service): 消息撤回时间边界容错——可配置偏差+反向偏差检查
815abbd feat(im-service): ACK丢失重推机制
a82b37b feat(im-common,im-service): 消息发送指数退避重试机制
```

### 测试结果

```
Tests run: 83, Failures: 0, Errors: 0, Skipped: 0 → ✅ 全部通过
```

| 模块 | 测试数 | 说明 |
|------|------|------|
| RetryUtilTest | 8 | 首次成功/重试成功/全部失败/业务不重试/默认参数/零重试 |
| PendingAckTrackerTest | 16 | 跟踪/确认/超时获取/条目行为/并发安全 |
| MessageSyncServiceTest | 13 | 原8个+新增5个（反向偏差/可配置容忍度/P2P成功/群聊成功） |
| P2PMessageServiceTest | 10 | 适配构造变更 |

### 待办事项（更新）

| 优先级 | 事项 | 状态 |
|------|------|----|
| ✅ | ~~消息发送重试 + 指数退避~~ | ✅ 已完成 |
| ✅ | ~~ACK 丢失重推机制~~ | ✅ 已完成 |
| ✅ | ~~消息撤回时间边界容错~~ | ✅ 已完成 |
| ✅ | ~~离线消息超限降级到 DB~~ | ✅ 已完成 |
| 🔴 | 前端 ↔ 后端接口联调 | ⏳ 待启动 |
| 🟡 | Redis/MQ 不可用时降级方案 | 📋 后续 |
| 🟡 | 日志链路追踪（TraceId） | 📋 后续 |
| 🟡 | 密码加密存储 | 📋 后续 |
| 🟢 | 群聊消息批量推送（性能优化） | 📋 后续 |
| 🟢 | MQ 削峰 + 本地缓存 | 📋 后续 |

---

## 2026-07-27 会话（第2轮）：离线消息超限降级至 DB

> 基于差距分析 L2 异常边界剩余任务
> 分支：feat/next-phase | 领先 master 13 个提交

### 本次完成工作

#### 离线消息 ZSet 超限降级到 DB

| 文件/模块 | 改动说明 |
|-----------|----------|
| `.../constants/ImConstants.java` | 新增 `OFFLINE_EVICTED_WATERMARK` 常量 |
| `.../message/.../MessageStoreService.java` | `evictIfExceeded` 从逐条删除改为**批量降级**超出限制的所有条目到 `message_history` 表；降级后记录水位线（max sequence）到 Redis，TTL 7天 |
| `.../message/.../MessageSyncService.java` | `syncOfflineMessage` 增强：检查降级水位线，当客户端 lastSequence 低于水位线时，同时从 `message_history` 表查询被降级的消息，用 HashSet 去重后合并返回 |
| `.../test/.../MessageSyncServiceTest.java` | 适配构造器新增 `ImMessageHistoryMapper` 和 `StringRedisTemplate` 参数 |

### Git 提交记录（本次新增）

```
727bc45 fix(im-service): 离线消息超限降级到 DB——批量降级+水位线+历史补全
```

### 测试结果

```
mvn compile: ✅ 通过
```

---

## 2026-07-27 会话（第3轮）：DB 写入失败补偿机制

> 基于差距分析 L2 异常边界剩余任务
> 分支：feat/next-phase | 领先 master 14 个提交

### 本次完成工作

#### DB 写入失败补偿队列 + 定时重试

当 MQ 不可用降级到直接写 DB 也失败时，不再丢消息，而是进入补偿队列定时重试。

| 文件/模块 | 改动说明 |
|-----------|----------|
| `.../MessageCompensationStore.java` | **新建** — 内存补偿队列，存储失败的 P2P/群聊消息，最多重试 5 次 |
| `.../MessageCompensationScheduler.java` | **新建** — @Scheduled 15s 定时重试，5 次耗尽后放弃并打印告警；包含幂等检查（跳过已写入的消息） |
| `.../MessageStoreService.java` | `storeP2PMessageDirectly` 和 `storeGroupMessageDirectly` 的 catch 块改走补偿队列；`extractToGroupMessageHistory` 改为 public |

### Git 提交记录（本次新增）

```
1fd52cf fix(im-service): DB写入失败补偿机制——内存补偿队列+定时重试
```

### 测试结果

```
mvn compile: ✅ 通过
```

---

> 文档版本: v1.3 | 创建时间: 2026-07-25 | 更新内容: 2026-07-27 DB写入补偿
