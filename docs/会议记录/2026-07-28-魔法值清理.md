# Vela IM — 会话工作记录

> 日期：2026-07-28
> 分支：feat/message-reply
> 状态：开发进行中

---

## 本次完成工作

### 魔法值清理

| 变更 | 说明 |
|----|------|
| `BusinessErrorCode` | **新建** — 统一业务错误码枚举（Bot/Admin/User/Group/Office/Document）|
| `MessageErrorCode` | 修复 50003 重复码（`MESSAGEBODY_IS_NOT_EXIST` → 50018）|
| `ImConstants` | 新增 `Bot` 和 `Notify` 常量分类 |
| `StatusConstants` | **新建** — 通用状态常量（ON/OFF/PENDING/DONE/REJECTED/ROLE 等）|
| `Result.fail(500, "xxx")` | 替换为 `BusinessErrorCode.*` 命名常量 |
| `setStatus(0/1/2)` | 替换为 `StatusConstants.*` 命名常量 |

### 涉及模块

| 文件 | 改动 |
|-----|-----|
| `BotService.java` | `setStatus(1)` → `BOT_ENABLED` |
| `AdminService.java` | `group.setStatus(1)` → `GROUP_DISSOLVED` |
| `AdminAuthService.java` | `admin.setStatus(1)` → `ON/OFF` |
| `ApprovalService.java` | `setStatus(0/1/2)` → `PENDING/DONE/REJECTED` |

## Git 提交记录

```
b1e8291 refactor: 统一错误码+状态常量+ImConstants整理
```

---

> 文档版本: v1.0 | 创建时间: 2026-07-28
