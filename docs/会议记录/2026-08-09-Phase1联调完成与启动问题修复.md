# Vela IM — 会话工作记录

> 日期：2026-08-09  
> 分支：feat/connectivity-test  
> 参与人：Demetrius + WorkBuddy

---

## 一、完成工作

### 1.1 Phase 1 全链路连通性测试 — ✅ 11/11 全部通过

| 步骤 | 接口 | 结果 |
|------|------|---|
| Step 1 | 导入用户 A | PASS |
| Step 2 | 导入用户 B | PASS |
| Step 3 | 导入好友关系 A→B | PASS |
| Step 3.1 | 导入好友关系 B→A | PASS |
| Step 4 | 验证好友关系 | PASS |
| Step 5 | 用户 A 登录 | PASS |
| Step 6 | 用户 B 登录 | PASS |
| Step 7 | 发送权限校验 | PASS |
| Step 8 | P2P 消息发送 | PASS |
| Step 9 | 同步用户 B 离线消息 | PASS |
| Step 10 | 同步用户 A 离线消息 | PASS |

### 1.2 message 服务首次启动成功（8014 端口）

修复 11 个启动阻断问题后，message 服务成功启动并通过全部 API 测试。

### 1.3 项目首次启动问题完整清单

产出文档：`docs/Vela项目启动问题完整清单.md`  
记录 10 大类、30+ 个启动阻断问题，含根因、影响范围和修复方案。

---

## 二、文件清单

### 新增文件（3 个）
| 文件 | 说明 |
|------|------|
| `docs/Vela项目启动问题完整清单.md` | 30+ 启动问题完整记录 |
| `docs/会议记录/2026-08-09-Phase1联调完成与启动问题修复.md` | 本文件 |
| `deploy/scripts/phase1_result.json` | 测试结果报告 |

### 修改文件（16 个，跨 8 个 commit）

| 文件 | 修复内容 |
|------|---------|
| `pom.xml` | 编译 target 8→21→8（Spring Boot 2.3.2 ASM 7.2 无法读取 Java 21 字节码） |
| `MessageReadService.java` | 2 处 `Stream.toList()` → `Collectors.toList()`（Java 16 API） |
| `GroupTagService.java` | 1 处 `Stream.toList()` → `Collectors.toList()` |
| `PersistAndPushNode.java` | 2 处 `List.of()` → `Collections.emptyList()`（Java 9 API） |
| `P2PMessageService.java` | 2 处 `List.of()` → `Arrays.asList()`/`Collections.singletonList()` |
| `SnowflakeIdWorker.java` | 补 `@Component` + 无参构造器（Spring 无法注入 long 参数） |
| `ImGroupMessageHistoryMapper.java` | 补 `@Mapper`（仅 `@Repository` 不足以被 MyBatis 识别） |
| `ImUserServiceImpl.java` | importUser 插入前查重（幂等性修复） |
| `ImFriendServiceImpl.java` | importFriendShip 插入前查重（幂等性修复） |
| `MessageApplication.java` | 4 次迭代修复：@MapperScan 扩展 common.entity → conversation → user → friendship；scanBasePackages 扩展 conversation → user → friendship |
| `vela-service-message application.yml` | 添加 `spring.main.allow-bean-definition-overriding=true`（跨模块 FeignClient Bean 冲突） |

---

## 三、Git 提交记录（feat/connectivity-test 分支）

```
1834fe4 docs: Vela 项目首次启动问题完整清单（30+ 启动阻断问题）
3769aae fix(im-service): message 服务允许多模块同名 FeignClient Bean 覆盖
0161371 fix(im-service): MessageApplication scanBasePackages/@MapperScan 覆盖 user/friendship 模块
7670ff3 fix(im-service): @MapperScan 覆盖 conversation 模块 Mapper 包
d51f9dd fix(im-service): MessageApplication scanBasePackages 添加 conversation 包
162eb16 fix(im-service): @MapperScan 扩展覆盖 com.vela.im.service.common.entity 包
3d2392e fix(im-service): ImGroupMessageHistoryMapper 缺 @Mapper 导致 message 服务启动失败
1fbc261 fix(im-service): SnowflakeIdWorker 加无参构造器，Spring 无法注入 long 参数
59703a8 fix(im-service): SnowflakeIdWorker 缺 @Component 导致 message 服务启动失败
2fb4764 fix(pom): 编译 target 8→21，与 JDK 21 运行环境对齐（后回退到8）
e863c63 fix(im-service): List.of() → Arrays.asList/Collections.emptyList/singletonList Java 8 兼容
d74e8cd fix(im-service): Stream.toList() → Collectors.toList() Java 8 兼容
4bbf7c6 chore: 添加 Phase 1 连通性测试脚本和中间件编排
2a3712f fix(im-service): Spring Boot 启动修复 — 注解、配置、依赖、循环依赖、数据层 (52 files)
```

---

## 四、待办事项

### 高优先级
- [ ] **message 模块跨服务依赖重构**：`CheckSendMessageService` 直接 import `ImUserService`/`ImFriendService`/`ConversationService`，应改为 Feign 客户端
- [ ] **FeignClient 命名冲突**：所有模块的 FeignClient 应添加 `contextId` 区分
- [ ] **Phase 2 TCP 协议测试**：TCP 客户端连接 → 登录 → 收发消息 → 心跳
- [ ] **Phase 3 浏览器端到端测试**：WebSocket 连接 → 登录 → 聊天 → 消息同步

### 中优先级
- [ ] **升级 Spring Boot**：2.3.2 → 2.5+ 以支持更高 Java 版本字节码（ASM 9.x 支持 Java 21）
- [ ] **添加单元测试**：项目目前零测试覆盖
- [ ] **CI/CD**：加入各模块启动检查（`@SpringBootTest`）防止回归

### 低优先级
- [ ] **DDD 分层重构**：消除 domain service 循环依赖（改用 Application Service 编排）
- [ ] **ZK 客户端升级**：zkclient → Apache Curator
- [ ] **清理硬编码**：workerId、密码等应从配置文件读取

---

## 五、当前运行状态

| 服务 | 端口 | 状态 |
|------|------|---|
| vela-gateway | 8889 | ✅ 运行中 |
| vela-service-user | 8010 | ✅ 运行中 |
| vela-service-friendship | 8011 | ✅ 运行中 |
| vela-service-message | 8014 | ✅ 运行中 |
| vela-tcp | 9000 | ✅ 运行中 |
| MySQL | 3307 | ✅ Docker |
| Redis | 6379 | ✅ Docker |
| RabbitMQ | 5672 | ✅ Docker |
| ZooKeeper | 2181 | ✅ Docker |
