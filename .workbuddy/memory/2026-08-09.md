## 2026-08-09

### importUser / importFriendShip 幂等性修复
- **问题**：重复运行 Phase 1 测试脚本时，user 和 friendship 服务的 import 接口直接 INSERT 导致 DuplicateKeyException
- **根因**：`ImUserServiceImpl.importUser()` 和 `ImFriendServiceImpl.importFriendShip()` 用 `mapper.insert()` 无脑插入，虽然 catch 了异常但 `e.printStackTrace()` 输出大量错误日志
- **修复**：插入前用 QueryWrapper 查是否存在，存在则跳过并放入 successId
- **影响文件**：
  - `vela-service-user/.../ImUserServiceImpl.java` — importUser 方法加存在性检查
  - `vela-service-friendship/.../ImFriendServiceImpl.java` — importFriendShip 方法加存在性检查

### Phase 1 测试结果 (01:12)
- **通过 7/11** (64%)
- Steps 1-6 全部通过: 用户导入、好友关系、登录
- Steps 7-10 失败: message 服务 (8014) 未启动，所有 /v1/message/* 返回 500
- importUser/importFriendShip 幂等性修复生效，successId 正确返回

### Git 提交 (feat/connectivity-test)
- `2a3712f` fix(im-service): Spring Boot 启动修复 (52 files)
- `4bbf7c6` chore: 测试脚本和中间件编排 (13 files)
- `d74e8cd` fix(im-service): Stream.toList() → Collectors.toList() Java 8 兼容 (2 files)
- `e863c63` fix(im-service): List.of() → Java 8 兼容 (2 files)
- `2fb4764` fix(pom): 编译 target 8→21→8（ASM 不兼容） (1 file)
- `59703a8` fix: SnowflakeIdWorker 缺 @Component (1 file)
- `1fbc261` fix: SnowflakeIdWorker 加无参构造器 (1 file)
- `3d2392e` fix: ImGroupMessageHistoryMapper 缺 @Mapper (1 file)
- `162eb16` fix: @MapperScan 扩展 common.entity 包 (1 file)
- `d51f9dd` fix: scanBasePackages 添加 conversation 包 (1 file)
- `7670ff3` fix: @MapperScan 覆盖 conversation Mapper 包 (1 file)
- `0161371` fix: scanBasePackages/@MapperScan 覆盖 user/friendship (1 file)
- `1834fe4` docs: 项目启动问题完整清单 (1 file)
- `3769aae` fix: 允许多模块同名 FeignClient Bean 覆盖 (1 file)
- 已推送至 origin/feat/connectivity-test

### Phase 1 测试结果 (02:02) — ✅ 11/11 ALL PASSED
- Steps 1-6: 用户导入、好友关系、登录 ✅
- Step 7: 发送权限校验 ✅
- Step 8: P2P 消息发送（返回 messageKey=503130582220801）✅
- Steps 9-10: 离线消息同步 ✅
- message 服务成功启动 (8014)，修复过程中额外发现并修复：
  - Stream.toList()/List.of() Java API 兼容
  - pom 编译 target 与 Spring ASM 版本冲突
  - SnowflakeIdWorker 缺 @Component + 构造器问题
  - ImGroupMessageHistoryMapper 缺 @Mapper
  - ConversationService 跨模块依赖导致 scanBasePackages/@MapperScan 不完整
  - FeignClient Bean 命名冲突
  - message 模块实为伪装微服务（直接 import 其他模块 domain service）
